version: '3.8'

services:
  backend:
    build:
      context: ./APIt
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ASPNETCORE_URLS=http://+:8000
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings=mongodb://host.docker.internal:27017
